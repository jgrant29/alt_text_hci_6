<%= form_with(model: alt_text) do |form| %>
  <% if alt_text.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alt_text.errors.count, "error") %> prohibited this alt_text from being saved:</h2>

      <ul>
        <% alt_text.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :alt_id, style: "display: block" %>
    <%= form.text_field :alt_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
