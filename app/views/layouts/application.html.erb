<!DOCTYPE html>
<html>
  <head>
    <title>Seven Army</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
     <meta charset="utf-8">
    <script src="https://kit.fontawesome.com/1922d83c51.js" crossorigin="anonymous"></script>
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:description" content="<%= @seo_description %>"/>
    <meta name="twitter:title" content="<%= @title %>" />
    <meta name="twitter:image:alt" content="<%= @twitter_alt %>">
    <% if @seo_image.present? %>
      <meta name="twitter:image" content="<%= @seo_image %>"/>
    <% end %>

    <meta property="og:type"          content="website" />
    <meta property="og:title"         content="<%= @title %>" />
    <meta property="og:description"   content="<%= @seo_description %>" />
    <% if @seo_image.present? %>
      <meta property="og:image" content="<%= @seo_image %>" />
    <% end %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    
  </head>

  <body class="min-vh-100">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <% if flash[:notice] %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= flash[:notice] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-labelledby="Close"></button>
      </div>
    <% elsif flash[:error] %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= flash[:error] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-labelledby="Close"></button>
      </div>
    <% elsif flash[:alert] %>
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <%= flash[:alert] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-labelledby="Close"></button>
      </div>
    <% end %>
    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">
        <%= link_to "Seven Army", alts_path, class: "navbar-brand" %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if user_signed_in? %>
          <% cid = current_user.id %>
          <% u = User.find_by(:id => cid) %>
           <% if u.super_user.nil? == false && u.super_user == true %>
            <li class="nav-item">
              <%= link_to "Alt Text", alt_texts_path, class: "nav-link" %>
            </li>
            <% end %>
          <% end %>
        </ul>
          
        <div class="d-inline"><%= link_to "Participate", verify_path, class: "me-3" %></div>
        <% if user_signed_in? %>
          <div class="nav-item">
            <a href="<%= new_alt_path %>" class="me-3">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-plus" viewBox="0 0 16 16">
                <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z"/>
              </svg> Create 
            </a>
          </div>
          <div class="d-inline"><%= link_to "Account", edit_user_registration_path, class: "text-white ms-1 btn btn-primary btn-sm" %></div>
          <div class="d-inline"><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "ms-1 btn btn-outline-primary btn-sm" %></div>
        <% else %>
          <%= link_to "Sign up", new_user_registration_path, class: "text-white ms-1 btn btn-primary btn-small"  %>
          <%= link_to "Login", new_user_session_path, class: "ms-1 btn btn-outline-primary btn-small"  %>
        <% end %>
      </div>
    </nav>
    
    <%= yield %>

    <footer class="footer fixed-bottom bg-light border-top">
      <div class="container">
        <span class="text-muted">2021 misfit-lab.com</span>
      </div>
    </footer>
  </body>
</html>
